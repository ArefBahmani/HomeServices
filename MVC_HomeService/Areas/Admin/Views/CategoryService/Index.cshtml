@using AppDomainCore.CategoryServiceEntitie.Entite


@model List<CategoryService>

<div class="col-lg-12">
    <div class="portlet box shadow">
        <div class="portlet-heading">
            <div class="portlet-title">
                <h3 class="title">
                    *زیرشاخه*
                </h3>
            </div><!-- /.portlet-title -->
        </div><!-- /.portlet-heading -->
        <div class="portlet-body">
            <div class="top-buttons-box mb-2">
                <a class="btn btn-success" asp-area="Admin" asp-controller="Create" asp-action="Index">
                    <i class="icon-plus"></i>
                    افزودن
                    <div class="paper-ripple"><div class="paper-ripple__background"></div><div class="paper-ripple__waves"></div></div>
                </a>
            </div>
            <div class="row">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover table-striped" id="data-table">
                        <thead>
                            <tr>
                                <th class="p-low"><input type="checkbox" id="btn-check-all-toggle"></th>
                                <th>#</th>
                                <th>تصویر</th>
                                <th>نام</th>
                                <th>هزینه پیشنهادی سایت</th>
                                <th>عملیات</th>
                                <th>شناسه شاخه </th>
                               

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in @Model)
                            {
                                <tr>
                                    <td><input type="checkbox"></td>

                                    <td>@item.Id</td>
                                    @if (string.IsNullOrEmpty(item.Image))
                                    {
                                        <td><img style="border-radius : 50%" src="~//AdminTemplate//images//user//placeholder.jpg" width="80" height="80" /></td>
                                    }
                                    else
                                    {
                                        <td><img style="border-radius : 50%" src="@item.Image" width="80" height="80" /></td>
                                    }


                                    <td>@item.Name</td>
                                    <td>@item.BidPrice</td>


                                    <td>
                                        <a asp-area="Admin" asp-controller="CategoryService" asp-action="Update" asp-route-Id="@item.Id" class="btn btn-info"><h3 style="color:yellow">ویرایش</h3> </a>
                                        <br />

                                        <a onclick=" return confirm('آیا از حذف این دستبندی اطمینان دارید؟')" asp-area="Admin" asp-controller="CategoryService" asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger"><h3 style="color:red">حذف</h3></a>
                                    </td>
                                    <td>@item.SubCategoryId</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div><!-- /.portlet-body -->
    </div><!-- /.portlet -->
</div><!-- /.col-lg-12 -->
@section Scripts
{
    <script src="~/AdminTemplate/plugins/data-table/js/jquery.dataTables.min.js"></script>
    <script src="~/AdminTemplate/js/pages/datatable.js"></script>
}